// LM Compliance System — small UI helpers
document.addEventListener('DOMContentLoaded', function () {
  // image tab toggles are wired inline in templates
  // auto-submit selects in inspection table handled inline
});
